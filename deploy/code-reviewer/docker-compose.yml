version: '3.9'
services:
  code-reviewer:
    build:
      context: ../..
      dockerfile: deploy/code-reviewer/Dockerfile
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8000}:${PORT:-8000}"
    volumes:
      - codereviewer-data:/data

volumes:
  codereviewer-data:
